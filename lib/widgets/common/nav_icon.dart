import 'package:flutter/material.dart';

class XNavIcon extends StatelessWidget {
  const XNavIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.arrow_forward_ios,
      size: 18,
      color: Color(0xFF999999),
    );
  }
}
